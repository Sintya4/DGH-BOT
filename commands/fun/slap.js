const snek = require('snekfetch');
const fsn = require('fs-nextra');
module.exports = {
        name: "name",
        usage: `usage`,
        category: "category",
        args: false,
        permission: "",
    run: async (client, message, args) => {
//code
}}